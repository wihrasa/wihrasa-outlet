'use strict';
/* nodejs modules */
/* npm modules */
/* local ( libraries ) modules */
const time      = require('../lib/time.js');
const db        = require('../lib/db.js');
const initToken = require('../lib/init.token.js');
/* local ( source codes ) modules */
/* modules implements */
const date = time(0);
/* global variables */
/* global functions */
initToken(token,(logged,status,data)=>{
  if(logged){
    //
  }else{io.to(id).emit('logout',status)}
});
/* the codes */
const outlet = (token)=>{
  //
  //
  return {
    omzet:omzet(db)
  }
  //
};

module.exports = outlet;
